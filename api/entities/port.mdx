---
title: "Port"
description: "Entity-layer endpoint used by spans for wire connections on poles."
---

## Overview

The `Port` struct represents a connection point on a pole where wires (spans) can attach. Ports are the runtime instances that correspond to slot templates defined in pole type definitions.

## Slots vs Ports

**Important distinction:**
- **Slots** are template-level definitions in `PoleTypeDefinition` that specify potential connection points
- **Ports** are entity-level instances created from slots (or generated by rules) that serve as actual connection endpoints for spans

A single slot can spawn multiple ports if `allow_multiple` is enabled in the slot template.

## Structure Definition

```cpp
struct Port {
  ObjectId id = kInvalidObjectId;
  std::string display_id{};
  ObjectId owner_pole_id = kInvalidObjectId;
  Vec3d world_position{};
  PortKind kind = PortKind::kGeneric;
  PortLayer layer = PortLayer::kUnknown;
  Frame3d direction{};
  ConnectionCategory category = ConnectionCategory::kLowVoltage;
  int source_slot_id = -1;
  int template_layer = 1;
  SlotSide template_side = SlotSide::kCenter;
  SlotRole template_role = SlotRole::kNeutral;
  bool generated_from_template = false;
  bool generated_by_rule = false;
  ConnectionContext placement_context = ConnectionContext::kTrunkContinue;
  bool angle_correction_applied = false;
  double side_scale_applied = 1.0;
  PortPositionMode position_mode = PortPositionMode::kAuto;
  PortPlacementSourceKind placement_source = PortPlacementSourceKind::kUnknown;
  bool user_edited_position = false;
  bool placement_override_flag = false;
  bool orientation_override_flag = false;
};
```

## Fields

<ResponseField name="id" type="ObjectId" default="kInvalidObjectId">
  Unique identifier for the port entity.
</ResponseField>

<ResponseField name="display_id" type="std::string">
  Human-readable identifier displayed in the UI.
</ResponseField>

<ResponseField name="owner_pole_id" type="ObjectId" default="kInvalidObjectId">
  Reference to the parent pole that owns this port.
</ResponseField>

<ResponseField name="world_position" type="Vec3d">
  3D position of the port in world coordinates.
</ResponseField>

<ResponseField name="kind" type="PortKind" default="kGeneric">
  Classification of the port type. See [PortKind enum](#portkind-enum).
</ResponseField>

<ResponseField name="layer" type="PortLayer" default="kUnknown">
  Infrastructure layer the port belongs to. See [PortLayer enum](#portlayer-enum).
</ResponseField>

<ResponseField name="direction" type="Frame3d">
  Directional frame indicating the port's orientation for wire connections.
</ResponseField>

<ResponseField name="category" type="ConnectionCategory" default="kLowVoltage">
  Connection category for wire type compatibility.
</ResponseField>

<ResponseField name="source_slot_id" type="int" default="-1">
  ID of the slot template that generated this port, or -1 if not from a template.
</ResponseField>

<ResponseField name="template_layer" type="int" default="1">
  Layer value from the source slot template.
</ResponseField>

<ResponseField name="template_side" type="SlotSide" default="kCenter">
  Side placement from the source slot template (left, center, right).
</ResponseField>

<ResponseField name="template_role" type="SlotRole" default="kNeutral">
  Role preference from the source slot template.
</ResponseField>

<ResponseField name="generated_from_template" type="bool" default="false">
  True if this port was created from a pole type slot template.
</ResponseField>

<ResponseField name="generated_by_rule" type="bool" default="false">
  True if this port was created by an automatic generation rule.
</ResponseField>

<ResponseField name="placement_context" type="ConnectionContext" default="kTrunkContinue">
  Context describing how this port is used in the network topology.
</ResponseField>

<ResponseField name="angle_correction_applied" type="bool" default="false">
  Whether corner angle correction was applied to this port's position.
</ResponseField>

<ResponseField name="side_scale_applied" type="double" default="1.0">
  Scale factor applied to lateral positioning at corners.
</ResponseField>

<ResponseField name="position_mode" type="PortPositionMode" default="kAuto">
  Whether port position is automatic or manually specified.
</ResponseField>

<ResponseField name="placement_source" type="PortPlacementSourceKind" default="kUnknown">
  Source that created this port. See [PortPlacementSourceKind enum](#portplacementsourcekind-enum).
</ResponseField>

<ResponseField name="user_edited_position" type="bool" default="false">
  Indicates if the user has manually edited this port's position.
</ResponseField>

<ResponseField name="placement_override_flag" type="bool" default="false">
  Flag indicating placement has been manually overridden.
</ResponseField>

<ResponseField name="orientation_override_flag" type="bool" default="false">
  Flag indicating orientation has been manually overridden.
</ResponseField>

## Related Types

### PortKind Enum

```cpp
enum class PortKind : std::uint8_t {
  kGeneric = 0,
  kPower = 1,
  kCommunication = 2,
};
```

Classifies ports by their functional type:
- **kGeneric**: Default/unspecified port type
- **kPower**: Power distribution connection point
- **kCommunication**: Communication/data connection point

### PortLayer Enum

```cpp
enum class PortLayer : std::uint8_t {
  kUnknown = 0,
  kHighVoltage = 1,
  kLowVoltage = 2,
  kCommunication = 3,
  kOptical = 4,
};
```

Defines infrastructure layers for port organization:
- **kUnknown**: Layer not specified
- **kHighVoltage**: High voltage power layer
- **kLowVoltage**: Low voltage power layer
- **kCommunication**: Communication/data layer
- **kOptical**: Optical fiber layer

### PortPlacementSourceKind Enum

```cpp
enum class PortPlacementSourceKind : std::uint8_t {
  kUnknown = 0,
  kTemplateSlot = 1,
  kGenerated = 2,
  kManualEdit = 3,
  kAerialBranch = 4,
};
```

Identifies how the port was created:
- **kUnknown**: Source not specified
- **kTemplateSlot**: Created from a pole type slot template
- **kGenerated**: Auto-generated by system rules
- **kManualEdit**: Created by user manual editing
- **kAerialBranch**: Created for aerial branch connections

### ConnectionCategory Enum

```cpp
enum class ConnectionCategory : std::uint8_t {
  kHighVoltage = 0,
  kLowVoltage = 1,
  kCommunication = 2,
  kOptical = 3,
  kDrop = 4,
};
```

Defines wire type categories for connection compatibility.

### ConnectionContext Enum

```cpp
enum class ConnectionContext : std::uint8_t {
  kTrunkContinue = 0,
  kCornerPass = 1,
  kBranchAdd = 2,
  kDropAdd = 3,
};
```

Describes the topological role of the port:
- **kTrunkContinue**: Port continues a main trunk line
- **kCornerPass**: Port at a corner where line changes direction
- **kBranchAdd**: Port where a new branch splits off
- **kDropAdd**: Port where a service drop is added

### SlotSide Enum

```cpp
enum class SlotSide : std::uint8_t {
  kLeft = 0,
  kCenter = 1,
  kRight = 2,
};
```

Lateral position of the port on the pole.

### SlotRole Enum

```cpp
enum class SlotRole : std::uint8_t {
  kNeutral = 0,
  kTrunkPreferred = 1,
  kBranchPreferred = 2,
  kDropPreferred = 3,
};
```

Hints for automatic port selection based on connection type.

## Usage Notes

- Ports serve as connection endpoints for [Spans](/api/entities/span)
- Each port belongs to exactly one [Pole](/api/entities/pole)
- The `source_slot_id` links back to the originating slot template in the pole type definition
- Multiple ports can be created from a single slot template if `allow_multiple` is enabled
- Port positioning can be automatic (based on context and pole orientation) or manual
